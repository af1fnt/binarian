// Kondisional Operator
let nama = prompt("Apakah Anda professor?");
let isAuthenticated = nama.toLowerCase() === "ya" ? "Anda memiliki akses" : "Anda tidak memiliki akses";
console.log(isAuthenticated);